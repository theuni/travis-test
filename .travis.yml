language: c
compiler: gcc
sudo: false
addons:
  apt:
    packages:
      - gcc-mingw-w64-x86-64
      - binutils-mingw-w64-x86-64
cache:
  apt: true
  directories:
  - cached
script:
  - mkdir cached
  - echo "test" > cached/cached.txt
