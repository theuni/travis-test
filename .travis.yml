language: c
compiler: gcc
sudo: required
dist: trusty
addons:
  apt:
    packages:
      - gcc-mingw-w64-x86-64
      - binutils-mingw-w64-x86-64
cache:
  apt: true
  directories:
  - cached
script:
  - echo "test" > cached/cached.txt
