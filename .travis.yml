language: c
compiler: gcc
sudo: false
addons:
  apt:
    packages:
      - gcc-mingw-w64-x86-64
      - binutils-mingw-w64-x86-64
      - wine
script:
  - command_not_found_handle() { if [[ $1 =~ .*.exe ]]; then wine "$@"; fi }
  - make check
