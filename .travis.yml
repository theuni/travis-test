language: generic
sudo: required
dist: trusty
install:
    - sudo apt-get update
    - sudo apt-get install --no-install-recommends --no-upgrade nsis g++-mingw-w64-i686 wine bc
script:
  - make check
