language: c
compiler: gcc
sudo: required
dist: trusty
addons:
  apt:
    packages:
      - gcc-mingw-w64-x86-64
      - binutils-mingw-w64-x86-64
cache:
  apt: true
  directories:
  - cached
script:
  - cat cached/cached.txt || echo "not yet cached"
  - echo "test" > cached/cached.txt
